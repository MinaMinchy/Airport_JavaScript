describe('Plane', function() {

   var plane = 'plane';
   var airport = new Airport();


describe('plane has landed', function() {
  it('instructs a plane to land', function() {
    expect(airport.instructsToLand(plane)).toEqual([plane])
  });
  });
  });
